# 维护指南

1. 在独立分支上修改源码；请使用自己选定的 Python 环境。
2. 实质性变更后运行 python scripts/run_tests.py。
3. 接口变更同步更新 README 和 Skill 工具参考。
4. 增加合成测试，不引入真实研究结果、病例信息、凭据或绝对本机路径。
5. 更改存储格式时提供兼容或迁移方案；不得静默覆盖历史版本。
6. 提交前检查 git diff --cached 和 git status。
7. 科学能力的主张需要独立评测；测试通过不得写成科研结论有效。

真实实验写入需另行设计运行产物登记、来源核验和用户授权流程。不要为方便演示解除 real 限制。
